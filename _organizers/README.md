# Organizers

One markdown file per organizer, 75 in all. These do not get their own
pages — they render inline on event pages (name, duties, photo where
available) via each event's `organizers:` list. Fields: `email`, `photo`.
