# Sponsors

One markdown file per sponsor, 32 in all. Rendered inline on event pages
via each event's `sponsors:` slug list — logo linked to the sponsor's site.
Fields: `link` (external URL), `logo`, `logo_white`, `about_short`,
`about_long`.
