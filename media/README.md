# Media

The site's original WordPress uploads at their original `YYYY/MM/` paths,
referenced in content as `/media/...`. `sites/6/` holds the uploads of the
2015.bilconference.com sub-site. Original files only; WordPress-generated
resize variants are not kept — browsers scale the originals.
